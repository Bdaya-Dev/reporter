name: example
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  reporter_syncfusion_flutter_xlsio: ^0.0.1+1
  cupertino_icons: ^1.0.2
  syncfusion_flutter_xlsio: ^22.2.7
  file_saver: ^0.2.7
  html: ^0.15.4
  reporter: ^0.1.2
  reporter_html: ^0.0.4
  collection: ^1.17.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
